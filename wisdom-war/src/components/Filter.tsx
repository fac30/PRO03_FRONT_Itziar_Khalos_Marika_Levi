


// Create the function Filter (the sidebar)
function Filter(props: FilterProps) {
    // inside add the filter logic
   
}

// Two list to keep track to the selected categuries and levels (State)
let selectedCategories = [];
let selectedLevels = [];

// define the categories and levels
let categories = ["FAC30", "Music", "Movies", "Art"];
let levels = ["Easy", "Medium", "Hard"];